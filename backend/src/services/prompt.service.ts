import { BaseService } from "./generic-base-service";
import { IPrompt } from "../models";

class PromptService extends BaseService<IPrompt>{

    // const createPrompt = async (promptData: Partial<IPrompt>) => {
    
    // }

}